export declare module '@mui/material' {
  interface Theme {
    themeToggler: () => void;
    layout: {
      contentWidth: number;
      minWidth: number;
      fullWidth: string;
      relative: string;
      absolute: string;
      middle: string;
    };
  }
  interface ThemeOptions {
    themeToggler: () => void;
    layout: {
      contentWidth: number;
      minWidth: number;
      fullWidth: string;
      relative: string;
      absolute: string;
      middle: string;
    };
  }
}
